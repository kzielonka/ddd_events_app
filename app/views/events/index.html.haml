.row
  .col-sm-12
    %h1.mb-4
      Buy event
    %table{ class: 'table table-hover' }
      %thead{ class: 'thead-dark' }
        %tr
          %th
            Title
          %th
            Description
          %th
            Total number of tickets
          %th
            Action
      %tbody
        - @events.each do |event|
          %tr
            %td
              = event.title
            %td
              = event.description
            %td
              = event.total_number_of_tickets
            %td
              = link_to 'Buy', admin_event_path(event.id)
